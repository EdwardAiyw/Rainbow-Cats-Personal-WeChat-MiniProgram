# 项目规则

- 这是微信云开发双人任务/心愿/商城小程序。先读 `README.md` 和 `miniprogram/config.js`，确认云环境后再联调。
- 客户端不直接读写数据库；数据变更通过云函数完成。新增或修改集合、字段、云函数时同步更新 README。
- 删除图片、页面或依赖前，先用 `rg` 确认没有 `.wxml`、`.js` 或配置引用，并在微信开发者工具中重新编译验证。
- 云函数部署使用“云端安装依赖”；不要提交密钥、个人身份信息或未脱敏的云端数据。
