<view class="container" wx:if="{{mission}}">
  <view class="page-heading">{{copy.pageTitle}}</view>
  <view class="card"><view class="detail-label">{{copy.title}}</view><view class="detail-value">{{mission.title}}</view></view>
  <view class="card"><view class="detail-label">{{copy.relation}}</view><view class="detail-value">{{from}} {{copy.started}} {{to}} {{copy.completed}}</view></view>
  <view class="card"><view class="detail-label">{{copy.reward}} · {{mission.credit}} {{copy.point}}</view><progress percent="{{creditPercent}}" color="#ff4e76" activeColor="#ff4e76" stroke-width="8" /><view class="muted">{{copy.maximum}} {{maxCredit}} {{copy.point}}</view></view>
  <view class="card"><view class="detail-label">{{copy.createdAt}}</view><view class="detail-value">{{dateStr}} {{timeStr}}</view></view>
  <view class="card"><view class="detail-label">{{copy.detail}}</view><view class="detail-value">{{mission.desc || '-'}}</view></view>
</view>
<view wx:else class="container empty"><view class="title">{{copy.notFound}}</view></view>
